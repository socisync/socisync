import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Socisync - Social Media Management for Agencies',
  description: 'Unified social media reporting and analytics platform for digital marketing agencies. Connect Meta, LinkedIn, YouTube & TikTok in one dashboard.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
